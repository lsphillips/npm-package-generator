// Replace the contents of this file with the entry point
// of your package tests.
// - - - - - - - - - - - - - - - - - - - - - - - - - - - -

import { expect } from 'chai';
import subject    from '../lib/${ package_name }.js';

// - - - - - - - - - - - - - - - - - - - - - - - - - - - -

describe('the subject', function ()
{
	it('shall greet the world', function ()
	{
		expect(
			subject()
		).to.equal('Hello World!');
	});
});
